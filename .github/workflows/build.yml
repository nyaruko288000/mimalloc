name: Build Mimalloc (Safe Mode)

on:
  push:
    branches: [ "master", "main" ]
  workflow_dispatch:

jobs:
  build-safe:
    runs-on: ubuntu-latest
    container: ubuntu:20.04
    
    env:
      DEBIAN_FRONTEND: noninteractive
      TZ: Asia/Shanghai

    steps:
    - name: Install Git and Essentials
      run: |
        apt-get update
        apt-get install -yq git wget software-properties-common tzdata

    - name: Checkout Source Code
      uses: actions/checkout@v4

    - name: Install Build Tools & Upgrade CMake
      run: |
        apt-get update
        apt-get install -yq python3-pip clang lld ninja-build \
          gcc-aarch64-linux-gnu g++-aarch64-linux-gnu \
          libc6-dev-arm64-cross binutils

        pip3 install cmake --upgrade

    - name: Build with CMake (Safe Flags)
      run: |
        mkdir -p build
        cd build
        
        # === 核心修改：安全保守模式 ===
        # 1. -O2 : 比 O3 更稳，不容易产生非法指令
        # 2. -march=armv8-a : 使用最通用的 ARMv8 指令集，不使用 A53 特性
        # 3. 移除了 -flto, -funroll-loops 等激进优化
        COMMON_FLAGS="-I/usr/aarch64-linux-gnu/include \
          -O2 \
          -march=armv8-a \
          -mtune=generic \
          -fomit-frame-pointer \
          -DNDEBUG \
          -fvisibility=hidden \
          -fno-math-errno \
          -Wno-unused-command-line-argument"
        
        MY_C_FLAGS="$COMMON_FLAGS -std=gnu17"
        MY_CXX_FLAGS="$COMMON_FLAGS"
          
        # 链接器也去掉 LTO
        MY_LDFLAGS="-L/usr/aarch64-linux-gnu/lib \
          -fuse-ld=lld \
          -Wl,-O2 -Wl,--icf=all -Wl,-z,relro,-z,now -s"

        cmake .. \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_SYSTEM_NAME=Linux \
          -DCMAKE_SYSTEM_PROCESSOR=aarch64 \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCMAKE_C_COMPILER_TARGET=aarch64-linux-gnu \
          -DCMAKE_CXX_COMPILER_TARGET=aarch64-linux-gnu \
          -DCMAKE_C_FLAGS="$MY_C_FLAGS" \
          -DCMAKE_CXX_FLAGS="$MY_CXX_FLAGS" \
          -DCMAKE_EXE_LINKER_FLAGS="$MY_LDFLAGS" \
          -DCMAKE_SHARED_LINKER_FLAGS="$MY_LDFLAGS" \
          -DCMAKE_MODULE_LINKER_FLAGS="$MY_LDFLAGS" \
          -DMI_BUILD_TESTS=OFF \
          -DMI_BUILD_SHARED=ON \
          -DMI_BUILD_STATIC=ON \
          -DMI_OVERRIDE=ON

    - name: Compile
      run: |
        cd build
        ninja -v

    - name: Verify
      run: |
        echo "架构检查："
        file build/libmimalloc.so

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: mimalloc-safe-mode
        path: |
          build/libmimalloc.so
